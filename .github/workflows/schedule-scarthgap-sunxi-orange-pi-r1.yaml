name: 'Schedule - sunxi-orange-pi-r1-scarthgap'

on:
  workflow_dispatch:
  schedule:
   - cron: "15 */8 * * *"

jobs:

  build-sunxi-orange-pi-r1-scarthgap:
    uses: ./.github/workflows/buildkas-starter.yaml
    with:
      configs: .github/configs/release/sunxi-orange-pi-r1-scarthgap.yaml:.github/configs/shared-vols.yaml
      machine_name: sunxi-orange-pi-r1-scarthgap
    secrets: inherit
