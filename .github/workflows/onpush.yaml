name: 'Build Basic'

on:
  push:

jobs:
  build-bsp:
    runs-on: ["self-hosted"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build Kas
        uses: ./.github/actions/kas-build
        id: build-kas

