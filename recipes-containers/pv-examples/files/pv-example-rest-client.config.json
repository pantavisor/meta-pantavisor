{
  "#spec": "service-manifest-run@1",
  "name": "rest-client",
  "services": {
    "required": [
      {
        "name": "network-manager",
        "role": "admin",
        "target": "/run/pv/services/network-manager.sock"
      }
    ]
  }
}
