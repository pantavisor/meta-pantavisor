{
    "PV_GROUP": "app",
    "PV_SERVICES_REQUIRED": [
        {
            "name": "system-bus",
            "type": "dbus",
            "role": "monitor",
            "target": "/run/dbus/system_bus_socket"
        }
    ]
}
